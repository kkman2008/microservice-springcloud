spring:
  application:
    name: eureka-client-1
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8081
management:
  endpoint:
    health:
      show-details: always
feign:
  hystrix:
    enabled: true